# 09. 스트레스 테스트 실측 결과

- 측정: 2026-07-30 02:52-05:47 UTC, strad32 (본 측정 5런 + 재현성 1런 + 플래그 A/B 1런 + DP=4 확장 1런)
- 대상: Qwen3.6-35B-A3B NVFP4, nginx LB `:8000`. 본 측정은 DP=2 (GPU 0-1), 이후 DP=4 (GPU 0-3)로 확장하여 재측정
- 하네스: [stress/](../stress/) · 설계: [specs/2026-07-30-vlm-stress-test-design.md](superpowers/specs/2026-07-30-vlm-stress-test-design.md)
- 원자료: [stress/results/](../stress/results/) (서버 원본은 `/data01/dmt/stress-results/`)

## 1. 결론 네 줄

1. **처리량 상한은 DP=2에서 720, DP=4에서 1,508 출력 tok/s** (혼합 워크로드). 포화점은 각각 동시 24와 64이며 그 이상은 지연만 늘어난다
2. **권고 동시성은 지연 요건이 정한다.** 처리량만 보면 24까지 올려도 되지만 그 지점 TTFT p95가 9초다. TTFT p95 6초 이내를 원하면 8이 상한이다
3. **확장 효율은 선형이다.** 1장 대비 2장 1.95배, 2장 대비 4장 2.09배. 레플리카 추가가 그대로 처리량이 되며 지연도 함께 개선된다
4. **`--max-num-seqs` 를 16에서 32로 올리면 처리량 17퍼센트 상승.** 별도 실험 레플리카로 A/B 확인. 대가는 preemption 소량 발생(861요청 중 4건)이며 실패는 없었다

## 2. 동시성별 실측 (혼합 워크로드, LB 경유)

시나리오 혼합은 텍스트 20퍼센트, 1MP 이미지 50퍼센트, native 4464x2160 이미지 30퍼센트다.
출력은 `max_tokens=256` 고정(`ignore_eos`), thinking 비활성.

| 동시 | RPS | 출력 tok/s | TTFT p50 | TTFT p95 | 전체 p95 | 실패 | running | waiting | KV |
|---|---|---|---|---|---|---|---|---|---|
| 1 | 0.47 | 114 | 0.9초 | 2.3초 | 3.5초 | 0 | 0.5 | 0 | 5% |
| 2 | 1.12 | 274 | 0.9초 | 2.4초 | 3.5초 | 0 | 1.4 | 0 | 5% |
| 4 | 1.36 | 341 | 0.9초 | 4.0초 | 5.5초 | 0 | 2.7 | 0 | 10% |
| 8 | 2.29 | 586 | 1.0초 | 5.6초 | 8.9초 | 0 | 6.1 | 0 | 21% |
| 12 | 2.12 | 546 | 1.0초 | 5.6초 | 11초 | 0 | 9.9 | 0 | 28% |
| **16** | 2.54 | **657** | 2.1초 | 7.1초 | 11초 | 0 | 13.2 | 0 | 33% |
| 24 | 2.85 | 717 | 1.5초 | 8.8초 | 15초 | 0 | 20.9 | 0 | 49% |
| 32 | 2.36 | 596 | 2.0초 | 8.9초 | 19초 | 0 | 28.6 | 0 | 64% |
| 48 | 2.83 | **720** | 7.5초 | 14초 | 27초 | 0 | 30.2 | 14.0 | 62% |
| 64 | 2.78 | 704 | 12초 | 20초 | 32초 | 0 | 30.4 | 30.2 | 64% |

- 포화점 16: 처리량이 최대치의 91퍼센트에 도달하는 첫 지점이다. 24에서 717, 48에서 720으로 사실상 평탄하다
- **48 이상에서는 처리량이 전혀 늘지 않고 TTFT만 7.5초에서 12초로 늘어난다.** 순수한 큐 대기다
- `running`이 30 근처에서 멈춘다. `--max-num-seqs 16` 곱하기 레플리카 2 = 32가 상한이며, 초과분은 `waiting`으로 쌓인다

### 재현성 확인 (단계 180초 재측정)

단계 90초로는 단계별 변동이 컸다. 8에서 32 구간만 단계를 180초로 늘려 다시 측정했다.

| 동시 | 90초 단계 | 180초 단계 |
|---|---|---|
| 8 | 586 | 513 |
| 12 | 546 | 559 |
| 16 | **657** | **557** |
| 24 | 717 | **721** |
| 32 | 596 | 672 |

- **최대 처리량은 재현된다** (720 대 721 tok/s)
- **포화점은 16이 아니라 24다.** 동시 16 지점의 변동이 18퍼센트(657 대 557)로 커서 90초 단계로는 판정할 수 없다
- 처리량 곡선만으로 권고 동시성을 정하기 어렵다는 뜻이며, 지연 기준을 함께 봐야 한다(아래 3번)

이후 플래그 A/B 등 비교 측정은 단계 180초 이상으로 한다.

## 3. 파괴점: 동시 64에서도 실패 0퍼센트

전 구간 실패율 0이다. TTFT p95가 20초, 전체 p95가 32초로 클라이언트 타임아웃(TTFT 60초, 전체 300초)에 닿지 않았다. LB 5xx도 없었다.

즉 이 구성의 파괴점은 64보다 위이며, 실무 상한은 에러가 아니라 **지연 요건이 결정한다.**

| 지연 요건 | 감당 가능한 동시성 |
|---|---|
| TTFT p95 5초 이내 | 4 |
| TTFT p95 7초 이내 | 8 |
| TTFT p95 10초 이내 | 16에서 24 |
| 지연 무관, 처리량만 | 48 |

## 4. DP=2 확장 효율 1.95배

같은 워크로드를 레플리카 1개(`:8001` 직결)에만 걸어 비교했다.

| | 최대 처리량 | 포화점 | 포화점 TTFT p50 |
|---|---|---|---|
| 레플리카 1개 | 370 tok/s | 12 | 1.8초 |
| **DP=2 (LB)** | **720 tok/s** | 16 | 2.1초 |

**1.95배.** P2P가 막힌 GeForce 박스에서 레플리카 방식을 고른 판단([04 3번](04-gpu-pinning-and-serving.md#3-서빙-토폴로지-tp-vs-pp-vs-dp))이 실측으로 확인됐다. 처리량이 부족하면 GPU 2-3에 레플리카를 추가하는 것이 가장 확실한 수단이다. 단 RAM 몫 제약이 있다([04 1번](04-gpu-pinning-and-serving.md#1-공용-모델-첫-가동-절차)).

LB 분배는 zone 수정 이후 균등하다. 이 수정 전이었다면 DP=2 효율은 1.0에 가까웠을 것이다.

## 5. 해상도가 처리량을 지배한다

시나리오를 하나씩 단독으로 돌린 결과다(동시 16, LB 경유).

| 시나리오 | 이미지 토큰 | 출력 tok/s | TTFT p50 | 전체 p95 |
|---|---|---|---|---|
| S1 텍스트만 | 0 | **1885** | 0.16초 | 2.2초 |
| S2 1MP (1024x1024) | 약 1.0K | **1614** | 0.37초 | 2.6초 |
| S3 native (4464x2160) | 약 9.4K | **206** | 9.2초 | 16초 |

- native 이미지는 텍스트 대비 **처리량 1/9, TTFT 57배**다
- 1MP는 텍스트 대비 처리량 86퍼센트를 유지한다. **1MP까지는 사실상 공짜다**
- 혼합 워크로드에서 native가 30퍼센트만 섞였는데 전체 처리량이 657 tok/s로, 텍스트 전용의 35퍼센트까지 떨어졌다

**해상도 사다리([06 해상도 사다리](06-inspection-agent-design.md#해상도-사다리), [07 Phase 1](07-inference-optimization-roadmap.md))가 최우선 최적화라는 것이 수치로 확인됐다.** 전처리에서 패널 크롭으로 1MP 부근을 맞출 수 있으면 처리량이 8배 가까이 오른다.

## 6. 병목은 KV가 아니라 max-num-seqs

기동 로그의 "최대 동시성 8.26x"는 32K 컨텍스트를 꽉 채운 요청 기준이다. 실제 요청은 1K에서 10K 토큰이므로 KV는 여유가 있었다.

| 지표 | 실측 | 판정 |
|---|---|---|
| `kv_cache_usage_perc` 최대 | **64퍼센트** | KV는 남았다 |
| `num_preemptions_total` | **0** (전 구간) | KV 압박 없음 |
| `num_requests_running` 최대 | **30.4** | `--max-num-seqs 16` × 2 = 32 상한에 닿았다 |
| `num_requests_waiting` | 동시 48부터 14, 64에서 30 | 초과분은 전부 큐 대기 |

따라서 **다음 최적화 1순위는 `--max-num-seqs` 상향(16에서 24 또는 32)이다.** KV에 여유가 있으므로 `--gpu-memory-utilization` 0.965 상향([08 8번](08-optimization-catalog.md#8-플래그-스택-실측-확정))보다 먼저 시도할 값이다. Mamba cache 블록 한계([02 Hybrid 주의](02-model-candidates.md))가 실제로 어디서 걸리는지도 이 실험에서 같이 확인된다.

## 6-1. max-num-seqs 16 대 32 A/B

6번의 가설(슬롯 상한이 병목)을 실제로 확인했다. 운영 레플리카를 재기동하면 dst에 다운타임이 생기므로, 유휴 배정 GPU(GPU 2)에 실험 레플리카를 따로 띄워 비교했다([scripts/exp-replica.sh](../scripts/exp-replica.sh)). 다른 플래그는 운영과 동일하다.

| 항목 | seqs 16 (`vlm-r0`) | seqs 32 (`vlm-exp-8003`) |
|---|---|---|
| KV 캐시 | 3.14 GiB (270,767 tokens) | 3.06 GiB (262,144 tokens) |
| 32K 요청 기준 동시성 | 8.26x | 8.00x |

슬롯을 2배로 늘려도 KV는 2.7퍼센트만 줄었다. 슬롯 상한과 KV 예산은 사실상 독립이다.

단일 레플리카 동일 조건 부하 결과(출력 tok/s는 `RPS × 256`, `ignore_eos` 고정 길이):

| 동시 | seqs 16 | seqs 32 | 변화 | seqs 16 TTFT p95 | seqs 32 TTFT p95 |
|---|---|---|---|---|---|
| 8 | 299 | 337 | +13% | 5.7초 | 7.2초 |
| 12 | 340 | 327 | -4% | 7.0초 | 5.7초 |
| 16 | 369 | 417 | +13% | 8.6초 | 7.8초 |
| 24 | 303 | 405 | **+34%** | 13.0초 | 12.0초 |
| 32 | 349 | 433 | **+24%** | **21.0초** | **9.7초** |
| 최대 | **369** | **433** | **+17%** | | |

- 고동시성에서 이득이 크다. 동시 32에서 처리량 24퍼센트 상승, TTFT p95는 21초에서 9.7초로 절반 이하다. 슬롯이 부족해 큐에 쌓였던 요청이 바로 스케줄되기 때문이다
- 저동시성(1에서 12)에서는 차이가 측정 변동 수준이다. 슬롯이 병목이 아닌 구간이므로 예상대로다
- **대가는 preemption이다.** seqs 32에서 861요청 중 4건(0.5퍼센트) 발생했고 seqs 16은 0건이었다. KV 예산은 그대로인데 동시에 얹는 시퀀스가 늘어 압박이 생긴다. 실패로 이어진 건은 없다
- Mamba cache 블록 한계([02 Hybrid 주의](02-model-candidates.md))로 기동이 깨지는 일은 없었다. seqs 32는 정상 기동하고 healthy를 유지했다

DP=2로 환산하면 약 866 tok/s가 기대된다. 적용 전에 preemption 허용 범위를 정하고, 중간값 24와 `--gpu-memory-utilization` 0.965 병행을 함께 볼 것을 권한다.

## 6-2. DP=4 확장 실측

dst와 협의하여 배정 4장 전부를 서빙에 쓰기로 정한 뒤 확장했다. 운영 레플리카를 재기동하지 않고 GPU 2와 3에 레플리카를 추가하는 방식이라 **다운타임은 없었다**([scripts/serve.sh](../scripts/serve.sh) `add`).

| | DP=2 (GPU 0-1) | DP=4 (GPU 0-3) |
|---|---|---|
| 최대 출력 처리량 | 720 tok/s | **1,508 tok/s** |
| 포화점 | 동시 24 | **동시 64** |
| `running` 최대 | 30.4 (16×2 상한) | **60.5** (16×4 상한) |
| 권고 동시성 (포화점의 70퍼센트) | 17 | **44** |

**2장 대비 2.09배.** 1장 대비 2장이 1.95배였으므로 4장까지 선형성이 유지된다.

같은 동시성에서 처리량과 지연이 동시에 개선된다(둘 다 단계 180초 측정).

| 동시 | DP=2 tok/s | DP=4 tok/s | DP=2 TTFT p95 | DP=4 TTFT p95 |
|---|---|---|---|---|
| 8 | 513 | 738 | 5.7초 | **4.1초** |
| 16 | 557 | 997 | 8.8초 | **7.5초** |
| 24 | 721 | 1075 | 8.7초 | **7.6초** |
| 32 | 672 | 1193 | 14초 | **10초** |
| 48 | - | 1322 | - | 11초 |
| 64 | - | **1508** | - | 11초 |
| 96 | - | 1382 | - | 13초 |

- 동시 96에서 처리량이 꺾이고 `waiting`이 28로 쌓인다. 슬롯 64개가 상한이므로 예상되는 지점이다
- 실패는 동시 96까지 0건이고 preemption도 0건이다
- 확장 후 RAM 상한 합은 194g이다(운영 2대 64g + 신규 2대 32g + LB 2g). 신규 레플리카를 32g로 둔 이유는 몫 225g을 넘기지 않기 위함이며, 실사용은 레플리카당 약 10g이라 여유가 충분하다

확장 절차에서 지켜야 하는 순서가 있다. **레플리카가 healthy가 된 뒤에 nginx upstream을 늘리고 reload해야 한다.** 순서를 뒤집으면 LB가 아직 죽은 포트로 요청을 보내 dst가 502를 받는다.

## 7. 타 팀 무침범 증빙

부하기는 GPU를 요청하지 않았고 cpuset은 `12-15,44-47`(dst+dmt 배정의 부분집합), NUMA 구역 0, RAM 상한 8g였다. 감시 프로세스가 1초 간격으로 타 팀 GPU와 서버 지표를 기록했다.

| 항목 | 5개 런 최악값 | 임계 | 판정 |
|---|---|---|---|
| 타 팀 GPU(4-7) 최고 온도 | 65도 | 83도 | 여유 18도 |
| 우리 GPU(0-1) 최고 온도 | 75도 | 참고용 | 정상 |
| 우리 GPU(0-1) 최고 전력 | 468W | 450W 캡 | 캡 동작 중 |
| load average 최대 | 17.5 | 96 | 여유 큼 |
| MemAvailable 최소 | 432 GB | 20 GB | 여유 큼 |
| 우리 컨테이너 메모리 최대 | 19.6 GiB | 몫 225 GiB | 여유 큼 |
| 부하기 컨테이너 CPU 최대 | 9퍼센트 | 참고용 | 측정 오염 미미 |

가드는 한 번도 발동하지 않았다. 타 팀 GPU의 SM 클럭 하락도 없었다(busy 표본 기준).

GPU 2-3은 전 구간 유휴로 유지했고, 타 팀 GPU와 포트에는 접근하지 않았다.

## 8. 한계

- 측정 중 타 팀의 `--cpuset-cpus` 없는 컨테이너가 우리 코어에 스케줄될 수 있었다([05 도커 사용 시](05-strad32-team-resource-split.md#도커-사용-시)). load average는 12에서 17.5 범위였고 우리 GPU util이 96퍼센트 이상이었으므로 GPU 바운드 구간의 수치는 영향이 작다. 다만 절대값은 이 조건에서의 실측이다
- 부하기 cpuset이 서빙 컨테이너 cpuset과 겹친다. 부하기 CPU 사용률이 9퍼센트 이하였으므로 영향은 작다
- 이미지는 합성이다. vision 토큰 수는 픽셀 수로 동일하지만 JPEG 크기가 실 프레임보다 작아 HTTP 본문 파싱 비용이 조금 덜 든다
- `run2`(레플리카 직결) 중 dst 트래픽이 섞였을 수 있다. `engine.csv`의 요청 수 증분으로 확인 가능하다
- 출력 길이를 256으로 고정했다. 실제 inspection 응답이 더 길면 처리량은 그만큼 내려간다
- 요청당 이미지를 1장으로 고정했다. 이 때문에 멀티이미지(4장) 긴 prefill 이 한 스텝을 16K 토큰으로 채우는 조합을 못 봤고, 이 조합이 2026-08-03 실사용에서 flashinfer MoE workspace OOM 으로 레플리카 4대를 전멸시켰다. 조치는 `--gpu-memory-utilization` 0.95 에서 0.93 하향, 근거는 [08 8번](08-optimization-catalog.md#8-플래그-스택-실측-확정)
- 집계 스크립트가 LB 뒤 레플리카 포트를 두 개로 가정하고 있었다. DP=4 첫 집계에서 처리량이 절반(696 tok/s)으로 나왔고, `engine.csv`에 기록된 포트를 그대로 쓰도록 수정한 뒤 1,508 tok/s로 정정했다. 감시 스크립트도 같은 이유로 수정했다(떠 있는 레플리카를 조회해 감시 대상을 정한다)
- `seqs32` 런의 `engine.csv`는 비어 있다. 감시 스크립트가 기본 레플리카(8001, 8002)만 보고 실험 레플리카(8003)를 보지 않아서다. 스크립트는 수정했고(대상 포트에서 감시 대상을 유도), 이 런의 처리량은 클라이언트 측 `RPS × 256`으로 산출했다. run2에서 이 방식과 엔진 측 수치가 일치함을 확인했다(369 대 370 tok/s)

## 8-1. 측정 후 정리 상태

측정에 쓴 자원은 전부 반납했다. 부하기 컨테이너 8개와 실험 레플리카를 제거하고
감시 프로세스와 임시 파일을 정리했다. 남은 것은 운영 서빙(`vlm-r0`, `vlm-r1`, `vlm-lb`)뿐이며
GPU 2와 3은 유휴, 우리 컨테이너 RAM 상한 합은 130g(몫 225g)으로 복귀했다.
포트도 8000에서 8002만 열려 있다.

Locust UI 데이터는 제거 전에 [stress/results/](../stress/results/) 로 내보냈다.

## 9. 다음 할 일

| 순위 | 항목 | 근거 |
|---|---|---|
| 1 | 해상도 사다리 확정 (패널 크롭으로 1MP 부근) | native가 처리량을 1/9로 떨어뜨린다. 효과가 가장 크다. 5번 |
| 2 | `--max-num-seqs` 32 적용 판단 | +17퍼센트 확인. preemption 0.5퍼센트를 허용할지 결정하고, 중간값 24도 함께 측정. 6-1번 |
| 3 | `--gpu-memory-utilization` 0.965 병행 A/B | seqs 상향으로 생긴 preemption을 KV 증량으로 상쇄할 수 있는지 확인 |
| 4 | 지연 요건 확정 후 권고 동시성 확정 | 파괴점이 아니라 지연이 상한을 정한다. 3번 |
| 5 | ~~DP=4 확장 판단~~ | **완료.** dst 협의 후 무중단 확장, 2.09배 확인. 6-2번 |

측정 방법에 대한 메모: 비교 측정은 단계 180초 이상으로 한다(2번의 재현성 확인). 실험 레플리카를 쓰면 운영 재기동 없이 A/B가 되므로, 플래그 변경은 이 경로로 검증한 뒤 운영에 반영한다.
